import React from 'react';
import EventExplorer from '../features/event-explorer/components/EventExplorer';

const EventExplorerPage: React.FC = () => {
  return <EventExplorer />;
};

export default EventExplorerPage;
